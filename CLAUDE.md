# 김인해 포트폴리오

## 프로젝트 개요
디자인 작업물을 소개하는 개인 포트폴리오 사이트입니다.

## 핵심 기능
- 자기소개
  - 프로필(이름, 소개 문구, 프로필 이미지), 경력/기술 스택 등 표시
- 작업물 목록
  - 썸네일, 제목, 간단한 설명을 카드/리스트로 나열
  - 목록에서 상세 페이지로 이동
- 작업물 상세
  - 이미지, 제목, 설명 등 상세 정보 표시
- 로그인
  - 관리자(소유자) 전용, 로그인해야 등록/수정/삭제 가능
- 작업물 등록/수정/삭제
  - 로그인한 상태에서 작업물 추가, 내용 변경, 삭제 처리

## 기술 스택
- 순수 HTML/CSS/JavaScript (프레임워크 없음)
- 데이터베이스/백엔드: Supabase (REST API 직접 호출 방식)
- 배포: GitHub 저장소 (`inhaekeem/day04`)에 push, `main` 브랜치 사용

## 현재 파일 구조
- `index.html` — 홈 페이지, 상단 공통 헤더/내비게이션 포함
- `about.html` — 소개 페이지
- `preview.html` — Supabase `test_todos` 테이블 데이터를 목록으로 보여주는 연습용 페이지
- `style.css` — 전체 페이지 공통 스타일 (세이지그린 컬러 팔레트 기반)

## 디자인 가이드
- 메인 컬러: 세이지그린 (`style.css`의 `--sage-*` CSS 변수 참고, `--sage-600`이 헤더/포인트 컬러)
- 공통 레이아웃: 모든 페이지는 `<header class="site-header">` + 내비게이션 + `<div class="page"><main>...</main></div>` 구조를 따름
- 새 페이지를 추가할 때는 `style.css`를 링크하고 기존 헤더/내비게이션 마크업을 재사용할 것

## 작업 시 참고사항
- 커밋 메시지는 한국어로 작성
- 위 핵심 기능(로그인, 작업물 등록/수정/삭제 등)은 아직 구현되지 않은 상태이며, `preview.html`은 Supabase 연동을 확인하기 위한 연습 페이지임
- 실제 포트폴리오 기능(작업물 목록/상세, 로그인, CRUD)을 구현할 때는 Supabase 테이블 스키마를 먼저 설계하고 인증(Auth) 연동 방식을 결정해야 함
